<UserControl x:Class="ChessGame.HoverButtonKinect"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="210">
    <UserControl.Resources>
        <Style x:Key="rectBack" TargetType="{x:Type Rectangle}">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="60"/>
            <Style.Triggers>
                
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
            <Rectangle Fill="#FFFFFF" HorizontalAlignment="Center" Height="60" VerticalAlignment="Center" Width="200" x:Name="rectBack"/>
            <TextBlock HorizontalAlignment="Center" Height="25" Margin="10,35,10,0" TextWrapping="Wrap" Text="Button Name" VerticalAlignment="Center" Width="180" FontSize="18" TextAlignment="Right"/>
        </Grid>
</UserControl>
